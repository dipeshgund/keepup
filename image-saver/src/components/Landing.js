const Landing = () => {
    return (
        <p>Go to Home to view Images.Click on save image to save a particular image.Go to Saved Images to view Saved images.</p>
    );
}
 
export default Landing;